/** @type {import('next').NextConfig} */
const nextConfig = {
  build: "next build",
  basePath: "/lp-andres",
  assetPrefix: "/lp-andres/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
